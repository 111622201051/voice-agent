# Core project dependencies (pinned ranges known to work)
webrtcvad-wheels>=2.0.14
torch>=2.0.0
torchaudio>=2.0.0
openai-whisper
silero-vad
sounddevice
soundfile
edge-tts
pygame
ollama
sqlalchemy
resemblyzer>=0.1.4
streamlit

# Web UI / REST API (ui/app.py + api/routes.py)
fastapi>=0.110
uvicorn>=0.27
python-multipart>=0.0.9

# Config & audio utility deps
pyyaml>=6.0
scipy>=1.10
librosa>=0.10

# Optional online voice cloning (Fish Audio SDK, imports as `fishaudio`)
fish-audio-sdk>=1.2.0

# Optional: fully-local voice cloning (F5-TTS, heavy) -> pip install -r requirements-voice-clone.txt