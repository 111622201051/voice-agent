# Shree Voice Agent - dependency manifest (pinned for Python 3.10-3.12, 64-bit)
# ---------------------------------------------------------------------------
# REQUIRED PYTHON: 3.10, 3.11 or 3.12 (64-bit). Do NOT use 3.13/3.14 yet -
# core wheels (pygame, torch, webrtcvad-wheels) are not published for them.
#
# EASIEST INSTALL (Windows): just double-click  setup_windows.bat
# It creates .venv, installs everything from requirements-lock.txt,
# handles resemblyzer/webrtcvad automatically, then tells you how to run.
#
# MANUAL INSTALL:
#   py -3.12 -m venv .venv
#   .venv\Scripts\activate
#   pip install webrtcvad-wheels==2.0.14
#   pip install -r requirements-lock.txt
#   pip install resemblyzer==0.1.4 --no-deps --no-build-isolation
#
# All versions below are the EXACT pinned build for Windows x64 / Python 3.12.
# Use requirements-lock.txt if you want every transitive package frozen too.

# --- Core engines ---
torch==2.9.1
torchaudio==2.9.1
openai-whisper==20250625
silero-vad==6.2.2
sounddevice==0.5.6
soundfile==0.14.0
edge-tts==7.2.8
pygame==2.6.1
ollama==0.6.2
sqlalchemy==2.0.54
streamlit==1.64.0

# --- Web UI / REST API ---
fastapi==0.141.1
uvicorn==0.53.0
python-multipart==0.0.32

# --- Config & audio utilities ---
pyyaml==6.0.3
scipy==1.18.1
librosa==1.0.0

# --- Online voice cloning (imports as `fishaudio`) ---
fish-audio-sdk==1.3.0

# --- webrtcvad (used by resemblyzer; binary wheels, avoids MSVC source build) ---
webrtcvad-wheels==2.0.14

# --- resemblyzer==0.1.4 (speaker verification) ---
# Installed separately by setup_windows.bat with `--no-deps`, because its
# dependency `webrtcvad>=2.0.10` only ships as a source tarball (it would
# require Microsoft Visual C++ Build Tools to compile). `webrtcvad-wheels`
# above provides the same module, and resemblyzer's other deps (librosa,
# numpy, scipy, torch) are already pinned in this file.